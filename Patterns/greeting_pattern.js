'use strict';

// Get the best matching response to the message
exports.getResponse = function() {
	return 'שלום קוראים לי אסף, אני אעזור לך למצוא את המוצר שאתה מחפש. \nאני רק צריך שתתאר לי אותו במילים פשוטות ואשלח לך את המציאות הכי שוות שאמצע.';
};

//   // Extract string from message object
//   const messageText = incomingMessageUtils.getTextFromMessage(message);
